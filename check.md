# 2026.04.01
- 특정 파일 `exiftool`로 확인한 후 `strings`를 사용해서 문자 확인.(특히나 image file)
- 압축 파일을 `unzip`으로 시도 후 실패할 경우 `7z`로 시도.
- `windows` 권한 상승에서 `dpapi` 폴더 확인.
- `powershell` 리버스 셸 생성시에 따옴표 신경쓰기.
- AD에서 특정 폴더 접근 가능 권한도 `icacls`로 확인 가능.

---
# 2026.04.03
- `/proc/self/environ`

---
# 2026.04.04
- URL에서 `+`는 공백을 의미하는데 정확하게 `+`를 사용하기 위해서 `%2B`로 전달.
- 파이썬에서 특정 모듈을 다운로드 받아서 테스트를 시도할 수 있다.
